@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}


@if (Model.DeviceType == Helpers.DeviceType.Desktop || Model.DeviceType == Helpers.DeviceType.Unknown)
{
    <div>Desktop</div>
}
else
{
    <div>Mobile</div>
}